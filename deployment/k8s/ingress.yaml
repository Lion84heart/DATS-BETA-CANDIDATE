---
# DATS-BETA-CANDIDATE Ingress Template
# Purpose: Routes external HTTP/HTTPS traffic to the ClusterIP service.
# NOTE: This is a template. Customize and uncomment sections before applying.
# Prerequisites:
#   1. An Ingress Controller (e.g., NGINX, Traefik) must be installed in the cluster.
#   2. DNS records must point to the Ingress Controller's external IP.
#   3. For TLS, install cert-manager or provide your own certificate.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dats-beta-candidate
  namespace: dats-beta-candidate
  labels:
    app.kubernetes.io/name: dats-beta-candidate
    app.kubernetes.io/component: ingress
  annotations:
    # --- NGINX Ingress Controller annotations (uncomment as needed) ---
    # kubernetes.io/ingress.class: "nginx"
    # nginx.ingress.kubernetes.io/ssl-redirect: "true"
    # nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    # nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    # nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
    # nginx.ingress.kubernetes.io/proxy-send-timeout: "60"
    # nginx.ingress.kubernetes.io/rewrite-target: /
    # nginx.ingress.kubernetes.io/rate-limit: "100"
    # nginx.ingress.kubernetes.io/rate-limit-window: "1m"
    
    # --- cert-manager TLS annotation (uncomment if using cert-manager) ---
    # cert-manager.io/cluster-issuer: "letsencrypt-prod"
    
    # --- Traefik annotations (alternative to NGINX) ---
    # traefik.ingress.kubernetes.io/router.entrypoints: "websecure"
    # traefik.ingress.kubernetes.io/router.tls: "true"
    
    # --- General annotations ---
    # kubernetes.io/ingress.allow-http: "false"
    # nginx.ingress.kubernetes.io/enable-cors: "true"
    # nginx.ingress.kubernetes.io/cors-allow-origin: "https://your-domain.com"
    # nginx.ingress.kubernetes.io/cors-allow-methods: "GET, POST, PUT, DELETE, OPTIONS"
    # nginx.ingress.kubernetes.io/cors-allow-headers: "Authorization, Content-Type"

spec:
  # --- TLS configuration (uncomment and customize) ---
  # tls:
  #   - hosts:
  #       - dats-beta-candidate.your-domain.com
  #     secretName: dats-beta-candidate-tls

  # --- Ingress rules (uncomment and customize) ---
  # rules:
  #   - host: dats-beta-candidate.your-domain.com
  #     http:
  #       paths:
  #         - path: /
  #           pathType: Prefix
  #           backend:
  #             service:
  #               name: dats-beta-candidate
  #               port:
  #                 number: 8000
  #   # Optional: additional host rules
  #   - host: api.dats-beta-candidate.your-domain.com
  #     http:
  #       paths:
  #         - path: /
  #           pathType: Prefix
  #           backend:
  #             service:
  #               name: dats-beta-candidate
  #               port:
  #                 number: 8000

  # --- Default backend (optional, for single-host setups without a hostname) ---
  # defaultBackend:
  #   service:
  #     name: dats-beta-candidate
  #     port:
  #       number: 8000
